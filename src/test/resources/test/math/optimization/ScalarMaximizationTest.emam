package test.math.optimization;

component ScalarMaximizationTest{
    implementation Math{
        maximize(Q x)
            Q z = - x * x + 42;
        subject to
            x >= -10;
            x <= 10;
        end
    }
}