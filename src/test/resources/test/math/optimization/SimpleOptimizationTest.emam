package test.math.optimization;

component SimpleOptimizationTest{
    implementation Math{
        minimize(Q x)
            Q y = x^2 - 2 * x + 1;
        subject to
            x >= 0;
        end
    }
}