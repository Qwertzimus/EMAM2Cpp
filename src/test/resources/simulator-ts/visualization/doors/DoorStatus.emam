package visualization.doors;

component DoorStatus {
    port
        in (0km/h:250km/h) velocity,
        out (0:1) status;

    implementation Math {

        B test = velocity < 1 km/h
        status = test;
    }
}