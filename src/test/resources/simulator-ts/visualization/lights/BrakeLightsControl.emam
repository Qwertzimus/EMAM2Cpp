package lights;

component BrakeLightsControl {
    port
        in (0s:1000s) time,
        out B status;

    implementation Math {

        // Turn brake lights on after 1 second
        status = time > 1s;
    }
}